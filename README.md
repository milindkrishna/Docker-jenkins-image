# Docker-jenkins-image

This repository contains the docker-compose file configuration needed to build and run the compose file.

Prerequisite:

create folder => 
volumes/master
volumes/node

below compose.yaml file
